{
  "ConnectionStrings": {
    "RentWiseProDb": "Server=DESKTOP-FRU391A\\SQLEXPRESS;Database=RentWisePro;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=True"
  },
  "Etl": {
    "MarkOffMarketAfterMissingRuns": 3,
    "MaxPhotosPerProperty": 10,

    // Force fixtures mode for local dev so we don't hit RapidAPI accidentally
    "UseFixtures": true,

    // Prefer Windows path; your Program.cs also supports combining current dir + Etl.Sources/Fixtures
    "FixtureRootPath": "Etl.Sources\\Fixtures",

    // Control which fixture scenario (folder) to read
    "FixtureScenario": "baseline",

    // Fixture sources no longer depend on RapidApi.Sources
    "FixtureSources": [
      "Fixture Listings"
    ]
  },
  "Storage": {
    "RawPayloadPath": ".local\\raw",
    "PhotoStoragePath": ".local\\photos"
  },
  "RapidApi": {
    // Leave blank in dev. Your Program.cs uses fixtures if ApiKey is empty OR UseFixtures=true.
    "ApiKey": "",

    // In fixtures mode, use Etl.FixtureSources instead of RapidApi.Sources.
    "Sources": []

    /*
    Production example (keep for later; do NOT enable until ready):
    "ApiKey": "__SET_VIA_USER_SECRETS_OR_ENV__",
    "Sources": [
      {
        "Name": "US Real Estate Listings",
        "Host": "us-real-estate-listings.p.rapidapi.com",
        "BaseUrl": "https://us-real-estate-listings.p.rapidapi.com",
        "EndpointTemplate": "/v2/properties?offset={page}&limit={pageSize}",
        "MaxRequestsPerMinute": 60,
        "MaxConcurrency": 2,
        "PageSize": 50
      }
    ]
    */
  },
  "EtlExecution": {
    // Keep default; scripts can override with --runOnce / --queue-only / etc.
    "Interval": "12:00:00"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.EntityFrameworkCore": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
